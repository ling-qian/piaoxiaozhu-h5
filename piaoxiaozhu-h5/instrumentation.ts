// Instrumentation hook for Next.js
// Sentry server-side init can be added here when NEXT_PUBLIC_SENTRY_DSN is configured
export function register() {
  // No-op — add Sentry server init here when ready
}
